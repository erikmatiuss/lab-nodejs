// Const Module

// Sum
const sum = (x , y) => x + y;

// Subtraccion

const Subtraccion = (x , y) => x - y;

// Multiplication

const Multiplication = (x , y) => x * y;

// Division

const Division = (x , y) => x / y;


module.exports = {
sum,
Subtraccion,
Multiplication,
Division,

};
